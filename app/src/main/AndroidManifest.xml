<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mayorapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icluncher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icluncher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.Activity.List_News_Activity" ></activity>
        <activity android:name=".View.Activity.news_Activity" />
        <activity android:name=".View.Activity.Users_Activity" />
        <activity android:name=".View.Activity.SurveyList_Activity" />
        <activity android:name=".View.Activity.AddSurvey_Activity" />
        <activity android:name=".View.Activity.MenuActivity" />
        <activity android:name=".View.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>